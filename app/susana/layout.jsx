export const metadata = {
  title: 'Formulario Oficial de Disculpas',
  robots: {
    index: false,
    follow: false,
  },
};

export default function SusanaLayout({ children }) {
  return children;
}
